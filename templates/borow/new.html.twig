{% extends 'base.html.twig' %}

{% block title %}New Borrow{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="text-center mb-4">
        <h1 class="display-4">Louer un livre</h1>
    </div>

    <div class="card shadow-sm p-4">
        {{ include('borow/_form.html.twig', { 'attr': {'class': 'needs-validation'} }) }}
    </div>
<br>
    <div class="text-center mt-4">
        <a href="{{ path('app_borow_index') }}" class="btn btn-secondary">Retour à la list </a>
    </div>
</div>
{% endblock %}
