{% extends 'base.html.twig' %}

{% block title %}Borrow{% endblock %}

{% block body %}
    <h1>Emprunt</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ borrow.id }}</td>
            </tr>
            <tr>
                <th>BorrowDate</th>
                <td>{{ borrow.borrowDate ? borrow.borrowDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ borrow.status }}</td>
            </tr>
            <tr>
                <th>Book</th>
                <td>{{ borrow.book.title }}</td>
            </tr>
            <tr>
                <th>User</th>
                <td>{{ borrow.user.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_borow_index') }}">Retour à la liste</a>

    <a href="{{ path('app_borow_edit', {'id': borrow.id}) }}">edit</a>

    {# {{ include('borow/_delete_form.html.twig') }} #}
{% endblock %}
