{% extends 'base.html.twig' %}

{% block title %}Author{% endblock %}

{% block body %}
    <h1>Author</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ author.id }}</td>
            </tr>
            <br>
            <tr>
                <th>FirstName</th>
                <td>{{ author.firstName }}</td>
            </tr>
            <br>
            <tr>
                <th>LastName</th>
                <td>{{ author.lastName }}</td>
            </tr>
            <br>
            <tr>
                <th>Bio</th>
                <td>{{ author.bio }}</td>
            </tr>
        </tbody>
    </table>
    <br>

    <a href="{{ path('app_author_index') }}">back to list</a><br>
    <br>

    <a href="{{ path('app_author_edit', {'id': author.id}) }}">edit</a><br>

    {{ include('author/_delete_form.html.twig') }}
{% endblock %}
